# Air fares digest — August 2026

Collection window `2026-08-01` to `2026-08-31`. Generated 2026-08-17 09:09 UTC by pipeline 1.0.0.

## Collection

_collection health unavailable: NotFound_

## By series

_per-series breakdown unavailable: NotFound_

## Reconstructions

_Jevons / departure-month / ONS target-time variant._

_No rows._

_None yet — reconciliation runs once ONS confirm the index day._

## Validation

**Verdict: INSUFFICIENT_DATA** — No reconstructed months carry a backfilled published_ons_value. Backfill from the ONS ad hoc release before scoring.

## ONS published series (the target)

- 678 values across 113 months (2016-01-01 to 2026-02-01), basis `annual_january_100`

## Needs attention

- Could not read collection health (NotFound). The table or view is missing — `ensure_tables` runs at the start of every pull, so this clears itself on the next collection run.
- Could not read per-series breakdown (NotFound). The table or view is missing — `ensure_tables` runs at the start of every pull, so this clears itself on the next collection run.
- Could not read route failures (NotFound). The table or view is missing — `ensure_tables` runs at the start of every pull, so this clears itself on the next collection run.
- Published ONS series ends 2026-02, 6 month(s) before this period, so there is no overlap to validate against yet. This closes only when ONS release a newer vintage; the monthly backfill workflow re-discovers releases automatically, so no action is needed here.

---

_Generated by `ukairfares.digest`. Committing this report is also what keeps the scheduled workflows alive: GitHub disables them after 60 days without repository commits, and workflow runs do not count._
